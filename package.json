{
  "name": "claw-proxy",
  "version": "0.1.0",
  "description": "Minimal OpenAI-compatible proxy for Claude Max subscriptions with multi-account rotation",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "claw-proxy": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch & node --watch dist/index.js",
    "test": "tsc && node dist/session.test.js && node dist/server_identity.test.js && node dist/server_rate_limit.test.js && node dist/adapter_output.test.js && node dist/adapter_resume.test.js && node dist/server_resume.test.js",
    "setup": "node dist/index.js --setup"
  },
  "dependencies": {
    "express": "^4.21.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.7.0"
  }
}
